function Rodape ( {autor = "josana"} ) {   //o autor entre chaves é um argumento que pode variar, mas nesse caso eu escolhi um valor padrão:josana
  return (
    <div>
      Copyleft by {"josana"}
    </div>
  )
}

export default Rodape;